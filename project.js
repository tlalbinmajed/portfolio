(async()=>{try{const projects=await fetch('projects.json').then(r=>{if(!r.ok)throw Error('Unavailable');return r.json()});const raw=new URLSearchParams(location.search).get('id');const id=raw!==null&&/^\d+$/.test(raw)?Number(raw):18;const p=projects[id];if(p){document.getElementById('project-name').textContent=p.name;document.getElementById('project-type').textContent=p.type;document.getElementById('number').textContent=String(id+1).padStart(2,'0');document.title=p.name+' — Project details';}}catch{document.getElementById('number').textContent='01';}})();
